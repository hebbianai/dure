# Generated remote Hmux resources

`pnpm app:dev` and `pnpm build` stage the two supported static Linux Hmux trees
here. The generated target directories are ignored and are bundled into Dure;
the Hmux source and installer remain the source of truth.
