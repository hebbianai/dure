[package]
name = "dure-provider-adapter"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
publish = false
description = "Reviewed native coding-agent CLI launch adapters for Dure"

[dependencies]
dure-app = { path = ".." }
serde_json = "1"
